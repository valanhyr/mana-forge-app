package com.manaforge.api.service;

import org.springframework.stereotype.Service;

/**
 * DEPRECATED placeholder: legacy Strapi client replaced by DirectusService.
 * Keep a minimal @Service bean to avoid missing-bean failures during the transition.
 */
@Service
@SuppressWarnings("unused")
public class StrapiService {
    // Intentionally minimal. Use DirectusService for actual CMS calls.
}
