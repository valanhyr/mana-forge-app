# Hosts for each service (override as needed)
API_HOST=http://localhost:8080
ENGINE_HOST=http://localhost:8000
WEB_HOST=http://localhost:5173

# Credentials for AuthenticatedUser scenarios in locustfile_api.py
# Create a test user in the database before running authenticated tests
TEST_USERNAME=testuser
TEST_PASSWORD=testpassword
